version: "3.9"

services:
  api:
    build: .
    container_name: stock-mood-api
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./stock_mood.db
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
    volumes:
      - .:/app
